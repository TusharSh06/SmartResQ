# ─────────────────────────────────────────
# Smart Resq — FINAL PRODUCTION BUILD
# Optimized for Railway (No Docker)
# ─────────────────────────────────────────

# ── Web Framework ────────────────────────
flask==3.0.3
flask-socketio==5.3.6
flask-cors==4.0.0

# ── Server ──────────────────────────────
# Pinned to 20.1.0 and 0.30.2 to fix the ALREADY_HANDLED import bug
gunicorn==20.1.0
eventlet==0.30.2

# ── Core CV + Image Processing ──────────
opencv-python-headless==4.9.0.80
numpy>=1.23.5,<1.27.0
pillow==10.3.0

# ── AI (CPU Optimized) ──────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
tensorflow-cpu==2.15.0

# ── OCR ─────────────────────────────────
easyocr==1.7.1
pytesseract==0.3.10

# ── Database ────────────────────────────
pymongo[srv]==4.6.3

# ── Auth & Security ─────────────────────
bcrypt==4.1.2
pyjwt==2.8.0

# ── Alerts / External APIs ──────────────
twilio==9.0.0
requests==2.31.0

# ── Config ─────────────────────────────
python-dotenv==1.0.1

# ── Utilities ──────────────────────────
gdown==5.1.0